let hackText = `Haxx0r ipsum unix cat python afk gobble exception regex gnu sql ip brute force I'm sorry Dave, I'm afraid I can't do that Linus Torvalds. Float while for fopen back door server overflow xss do char highjack vi cookie. Boolean throw interpreter daemon class all your base are belong to us packet *.* int /dev/null. Sudo data big-endian leet buffer finally rsa cd bytes eof January 1, 1970 I'm compiling. Private sudo man pages echo stack ssh hash strlen boolean race condition.

False wombat crack James T. Kirk ban gcc bang class leet function Trojan horse port. Try catch while all your base are belong to us ddos python dereference leapfrog fopen irc float client. Boolean access ssh socket suitably small values try catch true afk if error unix chown snarf syn case stack Donald Knuth lib int tera then xss tunnel in. Baz wabbit gnu void sql system bin stack trace foo flush script kiddies continue nak. Tcp less frack stdio.h hello world bit ip fail linux echo it's a feature todo pwned firewall semaphore bytes concurrently.

Malloc fopen linux /dev/null hexadecimal crack terminal stack trace alloc foo daemon buffer stdio.h it's a feature James T. Kirk gnu semaphore. Frack for machine code epoch bit true bin tarball ctl-c bar. Shell gc L0phtCrack all your base are belong to us. Ddos memory leak sudo dereference wabbit bang snarf system fork var loop protected kilo server vi over clock cat leapfrog try catch. Void stack client wombat root data win Linus Torvalds worm.

Break system ifdef leet sql cat bin vi stdio.h man pages tera unix public stack trace salt sudo xss ddos giga memory leak. Chown stack it's a feature script kiddies Dennis Ritchie int foo default cookie hash break overflow warez todo server back door syn mutex null. Linux flood L0phtCrack Leslie Lamport baz concurrently void cache worm I'm sorry Dave, I'm afraid I can't do that thread hexadecimal. Packet nak piggyback fail data finally ban printf mountain dew. True d00dz win ssh epoch spoof alloc fork afk emacs python strlen.

Segfault ban race condition stack ssh hack the mainframe highjack try catch foad null long. Giga echo bytes daemon packet sniffer fail dereference all your base are belong to us. Public it's a feature stack trace server Linus Torvalds data nak warez while pragma private socket irc case double headers infinite loop. Afk sql pwned grep bit Starcraft memory leak regex char syn script kiddies tcp eof public false bin endif protocol Donald Knuth int giga void. Todo boolean Leslie Lamport for linux flood frack continue semaphore I'm compiling.

Gnu daemon client echo while ban strlen eaten by a grue. Class foo perl error highjack Donald Knuth flood *.* public throw blob eof mutex system regex null else void sql leapfrog tunnel in. Function crack leet kilo piggyback ddos loop frack ascii /dev/null Starcraft gnu Linus Torvalds bypass if epoch root try catch bin ip long. Gc tera function double tcp socket buffer afk pragma gcc python salt cat firewall big-endian. Snarf float I'm sorry Dave, I'm afraid I can't do that lib bang it's a feature.`

let hackTextTarget = document.getElementById("hack_text_target");
let i = 0;
let bombBox = document.getElementById("bomb_box");
addEventListener("keydown", hackPrint);

function hackPrint(){
  hackTextTarget.textContent += `${hackText[i]}`;
  console.log(hackText[i]);
  i++;

}